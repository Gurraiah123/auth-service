server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://YOUR-RDS-ENDPOINT:3306/authdb
    username: admin
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
